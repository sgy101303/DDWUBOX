<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ddwucom.mobile.ddwubox">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".RatingActivity" />
        <activity android:name=".ReviewReadActivity" />
        <activity android:name=".ReviewWriteActivity" />
        <activity android:name=".CustomerCalendarActivity" />
        <activity android:name=".CustomerReservExptActivity" />
        <activity android:name=".AddActivity" />
        <activity android:name=".CustomerMainActivity" />
        <activity android:name=".AdminMainActivity" />
        <activity android:name=".CustomerShowActivity" />
        <activity android:name=".UpdateActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>